# Filesystem Layer Additions

## Local Instruction Files
Changes from local instruction files (such as `GEMINI.md` in the workspace root or subdirectories) are treated as filesystem-layer additions. These provide workspace-specific or directory-specific rules and instructions that take contextual precedence.
